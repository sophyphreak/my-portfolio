import React from 'react';
import SingleSection from '../SingleSection';
import projectList from './projectList';

const ReactApps = () => (
  <SingleSection projectList={projectList} sectionId="react-apps" />
);

export default ReactApps;
